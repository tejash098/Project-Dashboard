import PageLayout from "../layouts/PageLayout";

/**
 * Settings page — placeholder until settings form is added.
 */
const Settings = () => {
    return (
        <PageLayout title="Settings">
            <p>Settings page — coming soon.</p>
        </PageLayout>
    );
};

export default Settings;